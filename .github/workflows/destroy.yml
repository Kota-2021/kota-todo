# .github/workflows/destroy.yml 内の steps: ブロック

steps:
  - name: Checkout code
    uses: actions/checkout@v4

  # 1. ローカルのTerraformディレクトリを全て削除
  - name: 💣 Force Clean Local Terraform Cache
    run: |
      echo "Cleaning up local Terraform directories to reset environment..."
      rm -rf .terraform
      rm -f .terraform.lock.hcl
    working-directory: ./infra
      
  # 2. AWS認証情報の取得
  - name: Configure AWS Credentials
    uses: aws-actions/configure-aws-credentials@v4
    with:
      role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/GitHubActions-Terraform-Deployer
      aws-region: ap-northeast-1

  # 3. Terraform実行環境のセットアップ
  - name: Setup Terraform
    uses: hashicorp/setup-terraform@v3
    with:
      terraform_version: 1.13.5
      
  # 4. Stateファイルの強制クリーンアップ
  - name: 🧹 Final State Cleanup and Verification
    run: |
      # バックエンド設定の再適用（キャッシュをクリアするため）
      terraform init -force-copy
      
      # AWSへの問い合わせをスキップしてStateファイルの内容をクリア
      echo "Running destroy with -refresh=false to clear any lingering State entries..."
      terraform destroy -auto-approve -refresh=false
      
    working-directory: ./infra
    env:
      TF_VAR_db_password: ${{ secrets.DB_MASTER_PASSWORD }}